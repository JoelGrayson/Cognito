# Required: OpenAI checks your handwriting against the document (server-only).
OPENAI_API_KEY=
# Optional: override the vision model used for checking.
# OPENAI_MODEL=gpt-5
# Optional: API server port (Vite proxies /api here).
# PORT=8787
